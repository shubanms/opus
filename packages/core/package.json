{
  "name": "@opus/core",
  "version": "0.1.0",
  "description": "OPUS shared domain logic — framework-agnostic pure utilities used by both the web PWA (apps/web) and the React Native app (apps/mobile).",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./*": "./src/*.js"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
